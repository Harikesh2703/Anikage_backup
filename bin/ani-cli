#!/bin/bash
# Anikage wrapper script for Node.js version

# Get the directory where this script is located
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

# Run the Node.js version
exec node "$SCRIPT_DIR/anikage.js" "$@"
